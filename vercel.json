{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "./dist",
  "devCommand": "npm run start",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "./dist/index.js"
    }
  ]
}
