{
  "version": 2,
  "buildCommand": "npm i typescript; npm run build; npm run start",
  "outputDirectory": "./dist",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "./dist/index.js"
    }
  ]
}
