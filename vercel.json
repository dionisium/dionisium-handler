{
  "builds": [
    { "src": "package.json", "use": "npm install typescript && npm run build" }
  ],
  "routes": [
    { "src": "/dist/(.*)", "dest": "/index.js" }
  ],
  "env": {
    "NODE_ENV": "production"
  },
  "start": "npm run prod"
}