{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "vercel-sapper"}
  ],
  "routes": [
    { "src": "/vercel/output/(.*)", "dest": "/vercel/output/index.js" }
  ],
  "env": {
    "NODE_ENV": "production"
  },
  "installCommand": "npm install",
  "devCommand": "npm run start"
}